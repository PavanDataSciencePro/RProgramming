# store the values of a b and c to compute the quadratic expression
a=2
b=-1
c=-4
# store the values of computation in each of the variables
solution_1 = (-b + sqrt(b^2 - 4*a*c))/(2*a)
solution_2 = (-b - sqrt(b^2 - 4*a*c))/(2*a)
#print the values of each of these
print(solution_1)
print(solution_2)